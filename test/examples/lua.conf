events {
}
http {
    server {
        listen 80;
        location = /ok {
            content_by_lua_block {
                local _M = {}
                function _M.go()
                    -- }
                    ngx.say("Yoyoyoyo")
                end
                _M.go()
            }
        }
    }
}
